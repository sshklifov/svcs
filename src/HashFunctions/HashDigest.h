#ifndef HASH_DIGEST_INCLDUED
#define HASH_DIGEST_INCLDUED

#include <cstddef>
#include <vector>

extern size_t HashDigest (const std::vector<unsigned char>& digest);

#endif /* HASH_DIGEST_INCLDUED */
